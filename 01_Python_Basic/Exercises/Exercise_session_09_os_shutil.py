'''
======================================================================
                       Exercise 1: os module
======================================================================
'''
import os
import shutil

# Print current working directory

# List all files and directories in current location
# Show whether each item is a file or directory

# Create a new directory called "practice_folder"

# Navigate into the new directory

# Create a text file called "test.txt" with some content

# Go back to parent directory

# Copy the text file to a "backup" folder (create folder if needed)

# Display basic system information (OS name, CPU count)


'''
======================================================================
                       Exercise 1: shutil module
======================================================================
'''
import os
import shutil

# Execute a simple system command (like 'date' or 'ls')

# Run a command and capture its output

# Create a zip archive of a directory

# Show disk usage statistics for current directory

# Find the location of a common command (like 'python' or 'git')

# Create a simple script file and make it executable

# Extract the archive you created earlier
